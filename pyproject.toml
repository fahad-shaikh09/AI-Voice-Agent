[project]
name = "ai-voice-agent"
version = "0.1.0"
description = "Offline AI voice agent: Mic → Whisper → Qwen → Tools → Piper → Speaker"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "chainlit>=2.11.1",
    "ollama>=0.6.2",
    "faster-whisper>=1.0.0",
    "piper-tts>=1.2.0",
]
